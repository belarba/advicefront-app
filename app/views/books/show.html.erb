<h1 class="text-center"><strong><%= @book.title %></strong></h1>

<div class="info-area">
  <img class="img-left" src="<%= @book.coverUrl %>"/>
  <p class="text-justify"><%= @book.description %></p>
  <p class="mt-3 isbn"><strong>ISBN:</strong> <%= @book.isbn %></p>
</div>

<%= link_to 'Back', :back, class: 'mt-3 btn btn-secondary' %>

